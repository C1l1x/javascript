/* 
Napishete funkciq, koqto poluchava ime na chovek i otpechatva "Hello, <name>!", kudeto <name> e vuvedeno ime ot konzolata.
*/

function greetings(input) {
    let name = input[0];
    console.log(`Hello, ${name}!`);
}

greetings(["Simeon"]);